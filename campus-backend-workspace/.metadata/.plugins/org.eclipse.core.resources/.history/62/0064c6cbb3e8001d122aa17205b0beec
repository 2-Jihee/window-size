public class BusinessLogic {
    public int validateNum(int num) {
        if (num <= 0 || num >= 1000) {
            throw new IllegalArgumentException("유효하지 않은 숫자입니다.");
        }
        return num;
    }

    public void printNum(int dan) {
        System.out.println(dan + "단 입니다.");
        for (int i = 1; i < 10; i++) {
            System.out.println(dan + " * " + i + " = " + dan * i);
        }
    }
}
